apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-backup.fullname" . }}-test-connection"
  labels:
    {{- include "mysql-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
